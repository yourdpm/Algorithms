#include <iostream>
using namespace std;
struct qq {
	int n,a,b;
	int na,nb;
	int pr;
};
int main() {
	int q;
	cin >> q;
	qq p;
	for (int i=0; i<n; i++) {
		cin >> p[i].n >> p[i].a >>p[i].b; 
	}
	for (int i=0; i<n; i++) {
		int max = (p[i].a > p[i].b) ? p[i].a : p[i].b;
		while (n/max > 0) 
	}
	return 0;
}
